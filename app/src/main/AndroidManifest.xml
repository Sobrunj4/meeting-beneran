<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.meeting.tegal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:replace="android:allowBackup">
        <activity android:name=".ui.company.CompanyActivity"></activity>
        <activity android:name=".ui.maps.MapsActivity" />
        <activity android:name=".ui.detail_harga.DetailHargaActivity" />
        <activity
            android:name=".ui.available_room.AvailableRoomActivity"
            android:label="@string/title_activity_available_room"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.food_order.SelectFoodActivity" />
        <activity android:name=".ui.order.OrderActivity" />
        <activity android:name=".ui.password.PasswordActivity" />
        <activity android:name=".ui.detail_meeting.DetailMeetingActivity" />
        <activity android:name=".ui.meeting.MeetingActivity" />
        <activity android:name=".ui.register.RegisterActivity" />
        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.edit_profile.EditProfileActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Design.NoActionBar"></activity>
    </application>

</manifest>